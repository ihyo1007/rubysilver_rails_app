<div class="question-show-container">
  <div class="header-link">
    <%= link_to "前の問題へ戻る", question_path(@previous_question), class: "previous-question-link" if @previous_question %>
    <h1 class="question-title">問題<%= @question.id %></h1>
    <%= link_to "次の問題へ進む", question_path(@next_question), class: "next-question-link" if @next_question %>
  </div>
  <div class="question-area card">
    <pre><%= @question.content %></pre>
  </div>

  <div class="choices-area">
    <%= form_with url: answer_question_path, data: { turbo: false }, class: "answer-form" do %>
      
      <%= hidden_field_tag :question_id, @question.id %>

      <% @question.choices.each do |choice| %>
        <div class="choice-box">
          <label class="choice-label">
            <%= check_box_tag "choice_ids[]", choice.id, false, class: "choice-check" %>
            <span class="choice-text"><%= choice.content %></span>
          </label>
        </div>
      <% end %>

      <div class="button-area">
        <%= submit_tag "回答する", class: "answer-button" %>
      </div>

    <% end %>

    <div class="link-areas">
      <div class="banner-area">
        <%= link_to home_index_path do %>
          <%= image_tag "rubysilver_banner_yoko.png", alt: "RubySilver Banner", class: "banner_yoko" %>
        <% end %>
      </div>
      
      <nav class="icon-nav">
        <ul>
          <li>
            <%= link_to questions_path do %>
              <i class="fi fi-rr-edit"></i>
              <span>問題一覧</span>
            <% end %>
          </li>

          <li>
            <%= link_to mypage_path do %>
              <i class="fi fi-rs-book-alt"></i>
              <span>マイページ</span>
            <% end %>
          </li>
        </ul>
      </div>
    </div>
    
  </div>
</div>
