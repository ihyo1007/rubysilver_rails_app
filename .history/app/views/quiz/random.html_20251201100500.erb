<div class="question-show-container">
  <h1>問題</h1>
  
  <div class="question-area">
    <p><%= simple_format(@question.content) %></p>
  </div>
  
  <div class="choices-area">
    <%= form_with url: quiz_answer_path, data: { turbo: false } do %>
      <% @question.choices.each do |choice| %>
        <%= hidden_field_tag :question_id, @question.id %>
        <div class="choice-box">
          <label>
            <%= check_box_tag "choice_ids[]", choice.id %>
            <%= choice.content %>
          </label>
        </div>
      <% end %>
      <div class="button-area">
        <%= submit_tag "回答する", class: "button" %>
      </div>
    <% end %>
  </div>
  <%= link_to "ホーム", home_index_path, class: "btn btn-primary" %>
</div>